export enum TEventRequestStatus {
  PENDING,
  APPROVED,
  REJECTED
}

export enum TEventRequestSize {
  SMALL,
  MEDIUM,
  LARGE
}

export enum TEventPackageTierType {
  BRONZE, 
  SILVER,
  GOLD,
  PLATINUM
}